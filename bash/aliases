# misc
alias editdotfiles='mate ~/bin/dotfiles/'
alias ls='ls -la'

# location shortcuts
alias web='cd /library/webserver/documents/'
alias sites='cd ~/sites/'
alias marlytics='cd ~/sites/marlytics/'

# rails
alias resetdb='rake db:drop; rake db:create; rake db:migrate; rake db:seed; rake db:development:sample_data;'
alias migrate='rake db:migrate'

# git
alias add='git add .'
alias commit='git commit'
alias branch='git branch'
alias status='git status'