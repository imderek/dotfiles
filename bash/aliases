# misc
alias dotfiles='cd ~/bin/dotfiles/'
alias editdotfiles='mate ~/bin/dotfiles/'
alias ls='ls -la'
alias browser='/usr/bin/open -a "/Applications/Google Chrome.app"'

# location shortcuts
alias gems='cd ~/.rvm/gems/ruby-1.9.2-p0/gems/'
alias web='cd /library/webserver/documents/'
alias sites='cd ~/sites/'
alias marlytics='cd ~/sites/marlytics/'

# rails
alias resetdb='rake db:drop; rake db:create; rake db:migrate; rake db:test:prepare; rake db:seed; rake billing_plans:build; rake fonts:build; rake admin_users:build; rake db:development:sample_data --trace;'
alias migrate='rake db:migrate; rake db:test:prepare'
alias rollback='rake db:rollback'
alias routes='rake routes'

# heroku
# alias herokuresetdb='heroku pg:reset HEROKU_POSTGRESQL_BLACK --confirm marlytics; heroku rake db:migrate; heroku rake db:seed; heroku rake db:development:sample_data'
alias herokupull='heroku db:pull --exclude events,sessions,versions,visitors --confirm lawlytics'

# git
alias ga='git add --all; git status;'
alias gc='git commit -am '
alias gco='git checkout'
alias gb='git branch'
alias gs='git status'
alias push='git push'
alias pull='git pull'
alias log='git log --oneline'
alias uncommit='git reset --soft HEAD^'
alias recommit='git add --all; git commit -c ORIG_HEAD'
alias show='git show --pretty="format:" --name-only'
alias gst='git stash'
alias gm='git merge'
alias gd='git diff --cached'
alias mergedev='git checkout master; git merge development; git checkout development'